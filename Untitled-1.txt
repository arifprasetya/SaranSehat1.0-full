-- database: d:\PEMROGRAMAN\SaranSehatFlask\saransehat.db

-- Use the ▷ button in the top right corner to run the entire file.

CREATE TABLE foods (
    id INTEGER PRIMARY KEY,
    name TEXT,
    category TEXT CHECK(category IN ('sarapan', 'snack', 'makan_siang', 'makan_malam', 'minuman')),
    calories INTEGER
);
